# 认证与无人值守凭据设计

## 1. 目标

认证系统必须同时支持：

- 账号登录。
- 设备绑定。
- 临时验证码协助。
- 无人值守访问。
- 令牌撤销和审计。

所有凭据必须可撤销、可过期、可审计。

## 2. 账号登录

登录方式：

- 第一版使用邮箱 + 密码。

密码要求：

- 服务端只保存 Argon2id hash。
- 不保存密码明文。
- 登录失败必须限流。
- 多次失败后账号可临时锁定。

登录成功后返回：

- account_id。
- access token。
- refresh token。
- token 过期时间。

MFA：

- 账号启用 MFA 或风险验证要求 MFA 时，密码校验通过后不得直接签发 access token 或 refresh token。
- MFA 和风险验证流程以 `docs/spec/20260719190000001_账号MFA与风险验证设计.md` 为准。
- 高风险操作必须支持 step-up 验证，包括替换设备公钥、设备重绑定或设备密钥恢复、开启或轮换无人值守、关闭 `require_prompt`、授予或本地允许隐私屏、授予或本地允许本地输入保护、请求远程重启、发布客户端制品和修改企业区域驻留策略。

## 3. Token 策略

access token：

- 短有效期。
- 用于 API 请求和 WebSocket 鉴权。

refresh token：

- 长有效期。
- 只用于刷新 access token。
- 必须可撤销。
- 服务端只保存 hash。

首版固定默认：

- access token：15 分钟。
- refresh token：30 天。

规则：

- SaaS/V1 Core 默认值不得超过上述时间；如私有化部署需要调整，只允许缩短，放宽必须先更新安全设计和测试方案。

## 4. 设备绑定

设备绑定条件：

- 用户已登录。
- 客户端生成设备长期密钥对。
- 上传设备公钥。

设备绑定后：

- 设备属于账号。
- 设备可被账号名下主控端访问。
- 企业版再加入组织、角色和策略。

## 5. 临时验证码

用途：

- 技术支持和一次性协助。

规则：

- 被控端本地生成临时验证码。
- 被控端以本地临时验证码作为 OPAQUE 密码输入，与 api-server 完成 registration start/finish 后生成并注册 `code_verifier_record`。
- 临时验证码注册必须由该被控端设备签名请求发起；账号仅登录但没有目标设备私钥签名时，不能远程为设备创建临时验证码。
- 验证码短有效期。
- 验证码有最大尝试次数。
- 服务端不保存验证码明文。
- 服务端不得生成或返回验证码明文。
- 主控端不得向服务端提交验证码明文。
- 主控端不得在日志中记录验证码明文。

推荐默认：

- 6 位数字或更高强度短码。
- 有效期 5 分钟。
- 最大尝试 5 次。

## 6. 验证码校验

不得直接把验证码作为长期权限。

推荐流程：

```text
1. 被控端本地生成短码。
2. 被控端通过 `POST /v1/devices/{device_id}/temporary-codes` 与 api-server 完成 OPAQUE registration start/finish，不上传短码明文。
3. api-server 保存 `code_verifier_record`、`code_verifier_salt`、`proof_scheme`、过期时间和最大尝试次数。
4. 主控端输入设备 ID 和短码，通过 `POST /v1/sessions` 携带 `auth_method = temporary_code` 请求创建 `pending_code_verification` 会话。
5. api-server 校验设备和验证码状态后创建 pending session，并下发绑定该 `session_id` 的一次性 `code_challenge`。
6. 主控端通过 `POST /v1/sessions/{session_id}/temporary-code/verify` 完成 OPAQUE `proof_step = start | finish` 轮次，api-server 不接收短码明文。
7. 校验通过后把 session 标记为 `code_verified`，proof 失败则进入 `failed` 终态。
8. 被控端仍需确认。
9. 确认后会话才可连接。
```

短码注册：

- `POST /v1/devices/{device_id}/temporary-codes` 使用同一 API 路径承载 OPAQUE registration 轮次。
- `registration_step = start` 时，被控端提交 `proof_scheme`、`client_nonce`、`opaque_registration_request`、期望有效期和最大尝试次数，服务端返回 `registration_id`、`server_nonce`、`opaque_registration_response` 和过期时间。
- `registration_step = finish` 时，被控端提交 `registration_id`、`code_verifier_record`、`code_verifier_salt` 和 `transcript_hash`，服务端校验短期 registration state 后保存 verifier。
- 短码明文只允许进入被控端本地 OPAQUE 客户端计算；API、Signal、Relay、日志和审计上下文不得接收或保存短码明文。
- `verification:registration:{device_id}:{registration_id}` 保存 OPAQUE registration server state、`server_nonce`、`client_nonce`、过期时间、最大尝试次数和重放缓存；完成、过期或取消后必须清理。

首版短码注册消息字段：

```text
proof_scheme = opaque_ristretto255_sha512_v1
registration_step = start | finish
device_id
registration_id
server_nonce
client_nonce
opaque_registration_request
opaque_registration_response
code_verifier_record
code_verifier_salt
transcript_hash
expires_at_epoch_millis
max_attempts
```

短码证明：

- 首版唯一 `proof_scheme` 固定为 `opaque_ristretto255_sha512_v1`；更换或新增 proof scheme 必须先更新协议、服务端、客户端和测试文档。
- 证明上下文必须绑定 `device_id`、`code_id`、`controller_account_id`、`controller_device_id`、`session_id`、`server_nonce`、`client_nonce` 和过期时间。
- 服务端不得把短码 OPAQUE proof 结果当成长期权限。
- `POST /v1/sessions/{session_id}/temporary-code/verify` 使用同一 API 路径承载 OPAQUE 登录轮次：`proof_step = start` 时提交 `opaque_ke1`、`client_nonce`，服务端返回 `opaque_ke2`；`proof_step = finish` 时提交 `opaque_ke3`、`client_nonce` 和 `transcript_hash`，服务端验证通过后消费 challenge。
- 如果 `opaque_ristretto255_sha512_v1` 未实现，只能保留本地测试桩，不得把临时协助链路标记为通过验收。

首版短码证明消息字段：

```text
proof_scheme = opaque_ristretto255_sha512_v1
proof_step = start | finish
session_id
code_id
controller_account_id
controller_device_id
server_nonce
client_nonce
opaque_ke1
opaque_ke2
opaque_ke3
transcript_hash
```

challenge 生命周期：

- 每次 `code_challenge` 必须绑定唯一 `session_id`、`code_id`、`server_nonce`、过期时间和尝试次数。
- `proof_step = start` 后服务端 OPAQUE server state 只能保存在短期状态中，必须绑定 `session_id`、`code_id`、`server_nonce` 和 `client_nonce`。
- 同一 pending session 重新请求 challenge 时，旧 `server_nonce` 必须立即失效。
- 短码 OPAQUE proof 成功、验证码过期、尝试次数耗尽或 pending session 被关闭后，challenge 必须标记为 consumed/expired。

安全要求：

- 错误次数递减。
- 过期后不可用。
- 成功使用后立即消费。
- 同一设备验证码创建频率受限。

## 7. 无人值守访问

无人值守不是“永久免确认”，而是一种独立授权策略。

开启条件：

- 用户已登录。
- 如账号启用 MFA 或风险策略要求，已完成当前操作的 step-up 验证。
- 设备已绑定。
- 被控端本机用户主动开启。
- 设置无人值守访问凭据。
- 开启和轮换请求必须由目标被控端设备签名确认；远程账号或管理员只能关闭或禁用，不能绕过本机确认远程开启。

关闭条件：

- 本机用户关闭。
- 账号解绑设备。
- 管理员策略禁用。
- 凭据轮换或过期。

## 7A. 账号内设备确认连接

`auth_method = account_prompt` 用于同一账号或已授权账号内的设备连接。

流程：

```text
1. 主控端登录账号并选择在线设备。
2. 主控端通过 `POST /v1/sessions` 携带 `auth_method = account_prompt` 创建会话。
3. api-server 校验账号、设备归属或授权关系、有效权限和会话策略。
4. api-server 创建 `waiting_approval` 会话，并通过 Signal 推送被控端确认请求。
5. 被控端显式 `accept` 后，session 进入 `accepted`，才允许候选交换、Relay 分配、signed key exchange 和业务通道建连。
6. 被控端 `reject` 时进入 `closed`，主控端业务通道建立前 `cancel` 时进入 `cancelled`。
```

要求：

- `account_prompt` 不使用临时验证码或无人值守凭据。
- 被控端接受前不得交换候选、Relay token 或密钥。
- `accept/reject/cancel` 必须走 API 幂等写入口，Signal 只推送已持久化状态。

## 8. 无人值守凭据

服务端不得保存明文。

建议保存：

- `unattended_secret_id`
- `device_id`
- `credential_record`
- `credential_salt`
- `proof_scheme`
- `version`
- `enabled`
- `created_at_epoch_millis`
- `rotated_at_epoch_millis`
- `disabled_at_epoch_millis`

本地客户端保存：

- 加密后的本地配置。
- 系统安全存储优先。

证明方式：

- 首版唯一 `proof_scheme` 固定为 `opaque_ristretto255_sha512_v1`，服务端只保存 `credential_record`，不保存可直接重放的密码 hash；更换或新增 proof scheme 必须先更新协议、服务端、客户端和测试文档。
- 主控端不得把无人值守密码或等价长期密钥明文发送给 API、Signal 或 Relay。
- 每次无人值守连接必须由 api-server 下发一次性 `unattended_challenge`。
- 证明上下文必须绑定 `account_id`、`controller_device_id`、`controlled_device_id`、`session_id`、`unattended_secret_id`、`version`、`server_nonce`、`client_nonce` 和过期时间。
- 无人值守 OPAQUE proof 结果只能用于当前会话创建，不能复用到其他 session。
- `POST /v1/sessions/{session_id}/unattended/verify` 使用同一 API 路径承载 OPAQUE 登录轮次：`proof_step = start` 时提交 `opaque_ke1`、`client_nonce`，服务端返回 `opaque_ke2`；`proof_step = finish` 时提交 `opaque_ke3`、`client_nonce` 和 `transcript_hash`，服务端验证通过后消费 challenge。

凭据注册和轮换：

- `PUT /v1/devices/{device_id}/unattended-secret` 和 `POST /v1/devices/{device_id}/unattended-secret/rotate` 必须使用 OPAQUE registration start/finish 设置或轮换凭据。
- `registration_step = start` 时，客户端提交 `proof_scheme`、`client_nonce` 和 `opaque_registration_request`，服务端返回 `registration_id`、`server_nonce`、`opaque_registration_response` 和过期时间。
- `registration_step = finish` 时，客户端提交 `registration_id`、`credential_record`、`credential_salt` 和 `transcript_hash`，服务端校验短期 registration state 后保存新 `version`。
- 无人值守密码明文只允许进入本地 OPAQUE 客户端计算；API、Signal、Relay、日志和审计上下文不得接收或保存明文。
- 轮换必须先完成新 registration finish，再把旧 `version` 标记失效；任一步失败不得破坏旧凭据可撤销状态。

首版无人值守注册消息字段：

```text
proof_scheme = opaque_ristretto255_sha512_v1
registration_step = start | finish
device_id
unattended_secret_id
version
registration_id
server_nonce
client_nonce
opaque_registration_request
opaque_registration_response
credential_record
credential_salt
transcript_hash
```

首版无人值守证明消息字段：

```text
proof_scheme = opaque_ristretto255_sha512_v1
proof_step = start | finish
session_id
unattended_secret_id
version
account_id
controller_device_id
controlled_device_id
server_nonce
client_nonce
opaque_ke1
opaque_ke2
opaque_ke3
transcript_hash
```

## 9. 无人值守会话流程

```text
1. 主控端登录账号。
2. 主控端通过 `POST /v1/sessions` 携带 `auth_method = unattended` 请求连接设备。
3. api-server 校验账号和设备关系。
4. api-server 校验无人值守策略。
5. api-server 创建 `pending_unattended_verification` 会话，并下发绑定该 `session_id` 的一次性 `unattended_challenge`。
6. 主控端用无人值守凭据完成 `opaque_ristretto255_sha512_v1` start/finish 证明流程，通过 `POST /v1/sessions/{session_id}/unattended/verify` 提交 `opaque_ke1/opaque_ke3` 等公开轮次消息。
7. 服务端验证通过且有效权限 `require_prompt = false` 时，把 session 标记为 `unattended_verified`。
8. 服务端验证通过但有效权限 `require_prompt = true` 时，把 session 标记为 `waiting_approval`，并推送被控端确认请求。
9. proof 失败、过期或尝试次数耗尽时进入 `failed` 终态。
10. `unattended_verified` 可直接进入候选交换、Relay 分配、signed key exchange 和业务通道建连；`waiting_approval` 必须经被控端 `accept` 后才允许进入这些阶段。
```

要求：

- 被控端仍必须显示连接状态。
- 被控端仍必须可一键断开。
- 会话必须写审计。
- 无人值守成功连接、失败、轮换和关闭都必须写审计。
- `require_prompt = true` 时，无人值守 proof 成功不等于可建链，必须等待被控端 `accept`。
- 每次 `unattended_challenge` 必须绑定 `session_id`、`unattended_secret_id`、`version`、`server_nonce`、过期时间和尝试次数。
- `proof_step = start` 后服务端 OPAQUE server state 只能保存在短期状态中，必须绑定 `session_id`、`unattended_secret_id`、`version`、`server_nonce` 和 `client_nonce`。
- 同一 pending session 重新请求 challenge 时，旧 `server_nonce` 必须立即失效。
- 无人值守 OPAQUE proof 成功、失败耗尽、过期或 session 关闭后，challenge 必须标记为 consumed/expired，不能跨 session 重放。

## 10. 撤销

必须支持：

- 退出登录撤销当前 refresh token。
- 修改密码撤销所有 refresh token。
- 解绑设备撤销设备会话。
- 关闭无人值守撤销无人值守凭据。
- 管理员禁用账号或设备后拒绝新会话。

## 11. 验收

必须验证：

- 密码不会明文落库。
- refresh token 不会明文落库。
- 验证码错误次数限制有效。
- 验证码过期后无效。
- 验证码成功后不能重复使用。
- 验证码明文不会出现在 API、Signal、Relay 请求和日志中。
- 无人值守凭据明文不会出现在 API、Signal、Relay 请求和日志中。
- `proof_scheme` 不是 `opaque_ristretto255_sha512_v1` 时必须拒绝。
- 无人值守凭据证明不能跨 session 重放。
- 无人值守开启且 `require_prompt = false` 时，proof 成功后可进入允许建链状态。
- 无人值守开启且 `require_prompt = true` 时，proof 成功后必须进入 `waiting_approval` 并等待被控端接受。
- 无人值守凭据轮换后新凭据可用，旧凭据不可用。
- 无人值守关闭后旧凭据无效。
- 解绑设备后不能继续远控。
