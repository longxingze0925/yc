# AGENTS.md

## 项目身份

本项目是远程控制软件，后续主要由 AI Agent 协作开发。所有 Agent 必须优先交付真实、可运行、可验证的最小闭环，不为了形式扩展范围。

## 必读顺序

开始任何开发任务前，先阅读：

1. `README.md`
2. 本文件
3. `docs/spec/20260719170217641_产品规格说明书.md`（一手产品规格说明书的唯一权威源；旧...47669_远程控制软件产品技术规格.md` 仅供历史参考）
4. `docs/spec/20260719170217641_系统架构设计.md`
5. `docs/spec/20260719170217641_协议设计.md`
6. `docs/spec/20260719171718533_安全与密钥协议设计.md`
7. `docs/spec/20260719171718533_NAT穿透与连接状态机设计.md`
8. `docs/spec/20260719171718533_认证与无人值守凭据设计.md`
9. `docs/spec/20260719190000001_账号MFA与风险验证设计.md`
10. `docs/decision/20260721000000002_登录设备身份与受信任主控设备契约.md`
11. `docs/spec/20260719190000002_访问控制与条件访问设计.md`
12. `docs/spec/20260719190000003_隐私屏与本地输入保护设计.md`
13. `docs/spec/20260719190000004_供应链与客户端防伪设计.md`
14. `docs/spec/20260719193000001_反诈与滥用防护设计.md`
15. `docs/spec/20260719193000002_远程重启与会话恢复设计.md`
16. `docs/spec/20260719193000003_键盘IME与快捷键输入设计.md`
17. `docs/spec/20260719193000004_媒体质量控制与能力协商设计.md`
18. `docs/spec/20260719193000005_会话录制与合规审计边界设计.md`
19. `docs/spec/20260719193000006_企业区域路由与数据驻留设计.md`
20. `docs/spec/20260719170217641_数据库设计.md`
21. `docs/spec/20260719171718533_审计与权限数据模型修订.md`
22. `docs/spec/20260719170217641_客户端设计.md`
23. `docs/spec/20260719170217641_服务端设计.md`
24. `docs/plan/20260719170217641_测试方案.md`
25. `docs/plan/20260719170217641_里程碑计划.md`
26. `docs/plan/20260719170217641_AI开发任务清单.md`（一手任务表的唯一权威源；`docs/plan/20260719162947669_AI开发实施计划.md` 仅供历史参考）
27. 与当前任务直接相关的源码、测试和配置

说明：`docs/research/**` 不进入必读顺序，只作为竞品调研和背景参考；后续开发不得把 research 文档当作规范源。

## 当前阶段

用户已于 2026-07-21 明确确认文档定稿决策并授权开始开发。当前阶段进入 `V1 Core` 实施，按 `docs/plan/20260719170217641_AI开发任务清单.md` 的依赖顺序推进 M1-M7；M8 企业能力只能在 V1 Core 门禁通过后实施。

开发规则：

- 协议、数据库和安全字段必须以冻结文档为准，不得在实现中另起一套定义。
- 每个里程碑必须形成可运行、可验证的闭环，未通过门禁不得标记完成。
- Windows、Ubuntu X11 和 iOS 真机能力缺少对应环境时可以完成代码和替代验证，但必须保留发布阻断，不得伪造真机验收结果。
- 生产部署、生产迁移、Git commit/push 和签名发布仍需单独确认。

文档冻结必须满足：

- 首版远控业务通道明确使用应用层端到端加密，Relay/API/Signal 不得具备业务明文解密能力。
- 会话 X25519 临时公钥必须由设备长期私钥签名，防止 Signal/API 替换密钥材料。
- Signal WebSocket 账号 token + 设备签名握手流程已经写入协议文档。
- NAT 外部观测地址来源已经明确，不得用 WebSocket/TCP 远端地址替代 UDP P2P 候选。
- UDP observe 结果必须通过 `observe_result_id` 和绑定 hash 参与 `candidate_token` 签发校验，防止客户端自报公网候选。
- LAN Direct 候选也必须通过 `candidate_token` 授权，不能绕过会话授权直接探测。
- Relay token 必须绑定 `permissions_digest` 和 `relay_token_epoch`，并具备 TTL、撤销和短期授权状态校验。
- 协议设计与 NAT 设计中关于 `relay_open`、`session_relay_token`、`candidate_id` 和候选 token 的字段集合必须同步变更；任一字段变更都必须同时更新测试方案和 AI 任务清单。
- 远程重启 `api_request_hash` 的 canonical 字段已经冻结，被控端执行前必须校验。
- 临时验证码和无人值守首版 `proof_scheme` 固定为 `opaque_ristretto255_sha512_v1`。
- 账号 MFA、风险验证、条件访问、隐私屏、本地输入保护、客户端防伪、反诈与滥用、远程重启、键盘/IME、媒体协商、会话录制边界和企业区域驻留专项文档已经冻结。
- iOS 主控端最低支持版本冻结为 iOS 16；如未来提高最低版本，必须先补 ADR 并获得确认。
- iOS 真机网络矩阵必须在 `T021` 进入集成验收前补齐；设备不足时必须冻结替代用例并阻断 iOS 完整发布验收。
- 登录固定使用两段式 challenge；首段不得签发 token，首次设备注册必须消费绑定账号、设备、公钥和登录 challenge 的一次性 `device_enrollment_grant`，任意 access token 不具备新增设备权限。
- 无 MFA 账号可以自动注册设备但不得建立 trusted-device 记录；TOTP trust 最长 30 天、recovery-code trust 默认 24 小时，普通登录和 refresh 不得滑动续期。
- 用户已确认 `0001_initial_schema.sql` 未进入共享环境；账号身份 Schema 修订固定重写 `0001`，不得新增 `0002` 规避修订。

## 固定产品边界

本产品采用**统一客户端模型**：Windows 10/11 与 Ubuntu Desktop 26.04 LTS 的桌面客户端是同一个软件，登录账号后自动注册为账号内的一台设备，默认同时具备主控能力（远程控制其他设备）和被控能力（允许其他设备远程连接本机）。iOS 16 及以上是同一账号下的移动客户端，仅提供主控能力，不在第一版承担被控端远控。

桌面客户端按平台能力标记：

- `controller`：可以发起连接、控制其他设备。
- `controlled`：允许其他设备远程连接本机。
- `controller + controlled`：Windows/Ubuntu 默认同时具备，行为取决于本地权限、本机设置和会话授权。

平台边界：

- Windows 10/11：主控 + 被控
- Ubuntu Desktop 26.04 LTS x86_64：主控 + 被控
- iOS 16 及以上：仅主控

第一版不做 iOS 被控端完整远控，不做 Android，不做 macOS，不做浏览器被控端或浏览器主控端，不做远程开机/WOL/智能插座唤醒/带外管理唤醒，不承诺绕过系统权限的静默控制。

## 设备注册与登录的统一语义

桌面客户端安装后首次启动：

1. 登录前加载或生成本地设备长期 Ed25519 密钥对，展示登录界面并输入账号和密码。
2. 服务端校验密码后创建绑定设备身份、双 nonce、IP、User-Agent 和协议版本的登录 challenge，首段不签发 token。
3. 客户端使用设备私钥签名完成 challenge，并按要求完成 TOTP 或恢复码；服务端随后签发账号 token。
4. 未注册设备同时取得一次性、最长 5 分钟的 `device_enrollment_grant`，再以 token、grant 和拟注册私钥签名原子完成设备注册。
5. 客户端自动把当前桌面设备加入账号的"我的设备"列表；无 MFA 账号不因此建立 trusted-device 记录。
6. 当前桌面设备默认具备主控能力；是否具备被控能力取决于本机用户授权和系统权限采集授权。
7. 登录页必须明确展示提示："登录账号后，此设备会自动加入'我的设备'。"

iOS 客户端登录后：

1. 按同一 challenge/grant 流程注册为账号设备，能力固定为 `controller only`。
2. 只有本次完成设备签名和 TOTP/recovery 验证时才建立受信任主控设备记录；无 MFA 登录只注册设备。
3. 不出现"允许远程连接此 iOS 设备"的入口。
4. 在桌面设备列表中显示设备能力标记 `controller only`。

## 固定技术路线

未经明确架构决策变更，不得替换以下方向：

- 核心语言：Rust
- 桌面 UI：Slint + Rust
- iOS UI：SwiftUI/UIKit
- iOS 渲染：Metal
- iOS 解码：VideoToolbox
- Windows 渲染：D3D11 / DirectComposition
- Ubuntu 渲染：OpenGL / Vulkan，按驱动能力选择
- 主传输：QUIC
- 连接路径：P2P 优先，Relay 兜底，TLS 443 极端兜底
- 服务端：Rust axum + PostgreSQL + Redis
- 管理后台：SvelteKit

不得擅自改为 Electron、Flutter、纯 WebRTC、VNC/RDP 核心协议或单纯 TCP 长连接方案。

## 工程原则

- 先做最小可运行闭环，再扩功能。
- 协议、设备身份、安全边界优先稳定。
- 远控画面必须使用原生渲染层，不放在普通 UI 控件里高频绘制。
- 输入控制优先级高于视频清晰度。
- 文件传输不能抢占远控控制通道。
- Ubuntu 26.04 LTS 优先 Wayland/PipeWire，保留 X11 fallback。
- 所有权限敏感能力必须有显式开关和审计日志。
- 设备签名、签名密钥交换、验证码、无人值守凭据、MFA、条件访问、隐私屏、本地输入保护、NAT 穿透、客户端防伪、反诈与滥用、远程重启、键盘/IME、媒体协商、会话录制边界、企业区域驻留和审计模型必须先按专项文档冻结后再实现。

## 安全红线

禁止实现或默认开启：

- 隐藏运行远控
- 绕过用户授权
- 偷偷启用摄像头、麦克风或屏幕采集
- 绕过系统安全提示
- 静默获取密码、令牌或敏感文件
- 无审计的无人值守控制

必须实现或预留：

- 设备长期密钥对
- 账号身份和设备身份分离
- 首版远控业务通道使用应用层端到端会话密钥
- 临时验证码短有效期
- 无人值守密码本地加密保存
- 被控端明显连接状态提示
- 一键断开
- 黑名单
- 账号 MFA
- 反诈与滥用防护
- 条件访问策略
- 隐私屏独立授权
- 本地输入保护独立授权和断开恢复
- 远程重启的高风险验证、本地提示和新会话恢复
- 键盘、IME、组合键和断开释放按键状态
- 媒体能力协商、动态质量控制和输入优先策略
- 会话录制的 V1 禁止边界和 M8/V2 端点侧录制预留
- 企业区域路由和数据驻留的 M8 预留
- 客户端包签名、manifest 签名和 SBOM
- 文件传输独立授权
- 剪贴板独立授权
- 会话审计日志

## AI 开发方式

任务拆分建议：

- `Agent-Core`：Rust 核心库、协议、状态机、加密、跨平台接口
- `Agent-Desktop-Windows`：Windows 采集、输入、服务、渲染
- `Agent-Desktop-Ubuntu`：Ubuntu 采集、输入、服务、渲染
- `Agent-iOS`：iOS 主控、Metal 渲染、VideoToolbox 解码、触控映射
- `Agent-Server`：API、信令、Relay、数据库、在线状态
- `Agent-QA`：集成测试、弱网测试、连接矩阵、回归清单

同一文件同一时间只允许一个 Agent 写入。多个 Agent 分析同一模块时，只指定一个写入者，其他 Agent 提交建议。

## 验收要求

每个开发任务交付时必须说明：

- 改动文件
- 实际执行的验证命令
- 成功、失败或未执行的验证状态
- 已知风险
- 回滚方式

核心链路任务至少验证：

```text
进程启动
设备注册
信令连接
会话建立
断线重连
控制事件投递
资源释放
```

涉及远控画面时，必须验证：

```text
画面非空
分辨率正确
帧率稳定
鼠标/键盘输入可达
断开后被控端状态恢复
```

## Git 规则

不得擅自执行：

- `git init`
- `git commit`
- `git push`
- `git reset`
- `git rebase`
- 历史改写

需要这些操作时，必须先获得用户明确确认。
